# Note

Flutter/Dart nya di-install di env Windows, jd gk bisa make di wsl. Dir instalasi Flutter di `D:/TOOLS/DEVELOPMENT/flutter`, PATH bin di `D:/TOOLS/DEVELOPMENT/flutter/bin`. Extension Flutter/Dart jg udah keinstall di VS Code.